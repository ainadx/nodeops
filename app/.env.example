# NodeOps Console — environment
APP_NAME=NodeOps
APP_TAGLINE=One companion for the whole life of a Smart Traffic Node.
PORT=8000

# Repo link surfaced in the prototype banner + roadmap
NEXT_PUBLIC_REPO_URL=https://github.com/your-org/nodeops

# --- Production-only (not used by the reference build's mocks) ---
# DATABASE_URL=postgresql://user:pass@host:5432/nodeops
# MQTT_BROKER_URL=mqtts://broker.example.com:8883   # Device<->Cloud seam
# WEATHER_API_KEY=                                   # energy forecast (P2)
# PUSH_PROVIDER_KEY=                                 # APNs/FCM or provider
